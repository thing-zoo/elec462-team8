#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <stdlib.h>
#include <fcntl.h>

int sign_up(char*, char*); //sign up
int check(char*); //check id already exist 
int log_in(char*, char*); //log in
